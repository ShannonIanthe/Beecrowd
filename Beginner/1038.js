const input = require("fs").readFileSync("/dev/stdin", "utf-8").trim();
const value = input.split("\n");

const arr = [0, 4.00, 4.50, 5.00, 2.00, 1.5];

//first number is the price value in the index
//second number is how many you had of the product
function total(arr, index) {
    return value(arr[i]) * value;
}
total = arr[i] * value;

console.log(`Total: R$ ${total}`);